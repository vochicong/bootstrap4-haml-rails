machine:
  timezone:
    Asia/Tokyo

  environment:
    DOMAIN_NAME: example.com

dependencies:
  pre:
    - gem install bundler
    - bundle install --clean --with=test --without="development,production"
    # backup database.yml because CircleCI will override it
    - cp config/database.yml config/database.yml.bak

test:
  override:
    - bundle exec rails test
    # replace CircleCI's generated database.yml
    - cp config/database.yml.bak config/database.yml

deployment:
  dev:
    branch: /dev.*/
    commands:
      - sh deploy/gcloud-app-engine.sh
  staging:
    branch: master
    commands:
      - sh deploy/gcloud-app-engine.sh
