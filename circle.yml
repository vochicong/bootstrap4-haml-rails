machine:
  timezone:
    Asia/Tokyo

  environment:
    DOMAIN_NAME: example.com

dependencies:
  pre:
    - gem install bundler --pre
    - bundle install
    # backup database.yml because CircleCI will override it
    - cp config/database.yml config/database.yml.bak

test:
  override:
    - bundle exec rails test
    # replace CircleCI's generated database.yml
    - cp config/database.yml.bak config/database.yml

deployment:
  dev:
    branch: /dev.*/
    commands:
      - APP_VERSION=$CIRCLE_BRANCH sh deploy/gcloud-app-engine.sh
  staging:
    branch: master
    commands:
      - APP_VERSION=$CIRCLE_BRANCH sh deploy/gcloud-app-engine.sh
